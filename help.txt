docker exec -it wordpress_mariadb mariadb -u root -p<pass>

//remove all containers
docker rm $(docker ps -aq)

add bind to
/etc/hosts

sudo chown -R jpelline:jpelline /home/jpelline/data

verify nginx mime types
curl -I https://jpelline.42.fr/wp-includes/css/dist/block-library/style.min.css | rg -i "content-type"
